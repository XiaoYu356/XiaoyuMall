<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.user.mapper.UserMapper">
    
    <select id="selectByUsername" resultType="com.mall.user.entity.User">
        SELECT id, username, password, nickname, phone, email, avatar, gender, status, level, create_time, update_time, deleted
        FROM user
        WHERE username = #{username}
        AND deleted = 0
    </select>

    <select id="selectByPhone" resultType="com.mall.user.entity.User">
        SELECT id, username, password, nickname, phone, email, avatar, gender, status, level, create_time, update_time, deleted
        FROM user
        WHERE phone = #{phone}
        AND deleted = 0
    </select>
    
</mapper>
